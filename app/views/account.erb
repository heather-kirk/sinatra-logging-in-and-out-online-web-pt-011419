<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if self.is_logged_in?(session) %>
    <%= self.current_user(session).username %>
    <%= self.current_user(session).balance %>
    <a href="/sessions/logout">Log Out</a>
    <% else %>
    <h1>You Must <a href="/">Log In</a> to View Your Balance</h1>
      <% end %>
    <% end %>
  </body>
</html>